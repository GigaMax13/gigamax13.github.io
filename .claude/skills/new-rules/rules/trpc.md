# tRPC

Conventions live in the project-specific rule `.claude/skills/_rules/project-{PROJECT_ID}.md` (if present) and `.claude/skills/_rules/database.md` (soft-delete two-check, Prisma types import path).

Load on demand via `/load-rules`.
