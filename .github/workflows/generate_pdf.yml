name: Generate PDF from Markdown

permissions:
  contents: write

on:
  push:
    paths:
      - resume-short-version.md

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Pandoc
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex wkhtmltopdf

      - name: Check CSS file readability
        run: cat ./pandoc-style.css

      - name: Generate PDF from Markdown
        run: pandoc resume-short-version.md -o resume.pdf --css ./pandoc-style.css --pdf-engine=xelatex --metadata title="Resume"

      - name: Upload PDF to repository
        uses: actions/upload-artifact@v3
        with:
          name: resume-pdf
          path: resume.pdf

      - name: Commit PDF to repository
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          git add resume.pdf
          git commit -m 'Update resume PDF' || echo "No changes to commit"
          git push --force
